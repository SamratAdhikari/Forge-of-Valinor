const element = { name: "Happy", emoji: "😀" };

const Home = () => {
    return (
        <div className="w-full m-2 flex items-center justify-center gap-3">
            hi
        </div>
    );
};

export default Home;

// HOME <LogoutButton />
